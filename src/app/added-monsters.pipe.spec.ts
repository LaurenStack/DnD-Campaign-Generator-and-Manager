import { AddedMonstersPipe } from './added-monsters.pipe';

describe('AddedMonstersPipe', () => {
  it('create an instance', () => {
    const pipe = new AddedMonstersPipe();
    expect(pipe).toBeTruthy();
  });
});
