import { MonsterSearchPipe } from './monster-search.pipe';

describe('MonsterSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MonsterSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
