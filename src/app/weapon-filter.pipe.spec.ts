import { WeaponFilterPipe } from './weapon-filter.pipe';

describe('WeaponFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WeaponFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
