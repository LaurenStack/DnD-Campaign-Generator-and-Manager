import { MountsFilterPipe } from './mounts-filter.pipe';

describe('MountsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MountsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
